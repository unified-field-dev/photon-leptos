# Infra

**AWS MCP bench campaigns** live in the private sibling checkout:

`~/unified-field/uf-live-cloud-lab/photon-leptos/infra/aws/mcp`

Set `UF_LAB_ROOT` and see that repo’s README. Report archives:
`uf-live-cloud-lab/photon-leptos/reports/`.
